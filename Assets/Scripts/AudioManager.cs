using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioManager : MonoBehaviour
{
    public AudioClip[] clips;
    // Start is called before the first frame update
    //void Start()
    //{
    //    int i = 0;
    //    string[] audioPaths = System.IO.Directory.GetFiles("Assets/Audio/" + directory);
    //    clips = new AudioClip[audioPaths.Length / 2];
    //    foreach (string f  in audioPaths)
    //    {
    //        if (!f.Contains(".meta"))
    //        {
    //            clips[i] = (AudioClip)Resources.Load(f);
    //            i++;
    //        }
    //    }
    //}
}
